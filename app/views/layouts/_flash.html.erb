<% flash.each do |type, msg| %>
  <div class="alert alert-dismissible <%= bootstrap_class_for_flash(type) %>">
  <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    <%= msg %>
  </div>
<% end %>